generate random password using python: 


import random
import string
    

def create_password(n):
   allChars=list(string.ascii_letters)+list(string.digits)+list(string.punctuation)
   passphrase=[]
   for i in range(n):
      tmp=random.choice(allChars)
      passphrase.append(tmp)
   res="".join(passphrase)
   return res

test1=create_password(8)
print(test1)